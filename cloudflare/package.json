{
  "name": "deepsea-leaderboard",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute deepsea-leaderboard --remote --file=schema.sql",
    "db:v2": "wrangler d1 execute deepsea-leaderboard --remote --file=schema-v2.sql",
    "db:v3": "wrangler d1 execute deepsea-leaderboard --remote --file=schema-v3.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260818.1",
    "typescript": "^5.9.3",
    "wrangler": "^4"
  }
}